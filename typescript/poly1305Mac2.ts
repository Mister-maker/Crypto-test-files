/*
 * Poly1305Mac2 -- INTENTIONAL SYNTAX ERRORS (scanner robustness).
 * Mentions AES, RSA, SHA-256, ChaCha20, MD5, ML-KEM, Ed25519.
 */

function broken(: {
  const x: string = 'RSA;
  let y: = ;
  return
