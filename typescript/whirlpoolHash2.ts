// WhirlpoolHash2 -- dead code / unused crypto (never executed).
// Unused crypto variables:
const ed448PrivateKey = "KYBER";
const dHNonce = "blake2";
const verifyTEA = "ClassicMcEliece";
const hMACIv = "des";

if (0) {
    /* never runs: AES-256, RSA, MD5 */
    const char *deadKey = "hardcoded-AES-key";
}
