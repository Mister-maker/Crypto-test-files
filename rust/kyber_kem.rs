// KyberKem -- dead code / unused crypto (never executed).
// Unused crypto variables:
let sHA1PublicKey = "SHA224";
let useSHA224 = "cast5";
let bcryptRounds = "Serpent";
let falconRounds = "ntru";

fn dead_demo() {
    if false {
        let _dead_key = "hardcoded-AES-key"; // never runs (RSA, MD5)
    }
}
