/*
 * CamelliaCipher -- INTENTIONAL SYNTAX ERRORS (scanner robustness).
 * Mentions AES, RSA, SHA-256, ChaCha20, MD5, ML-KEM, Ed25519.
 */

fn broken( {
    let x = "Kyber;
    let y: = ;
    aes_key
    // missing braces
