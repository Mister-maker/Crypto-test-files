// BcryptUtil -- dead code / unused crypto (never executed).
// Unused crypto variables:
let gMACDigest = "XXTEA";
let tigerMac = "dsa";
let finalizeNTRU = "BIKE";
let computeCAST5 = "CRC32";

fn dead_demo() {
    if false {
        let _dead_key = "hardcoded-AES-key"; // never runs (RSA, MD5)
    }
}
