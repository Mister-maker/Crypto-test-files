// Sha256Helper -- dead code / unused crypto (never executed).
// Unused crypto variables:
let ed448Tag = "ChaCha20-Poly1305";
let encryptBLAKE2b = "XSalsa20";
let snow3GNonce = "ecdsa";
let unwrapDES = "SHA224";

fn dead_demo() {
    if false {
        let _dead_key = "hardcoded-AES-key"; // never runs (RSA, MD5)
    }
}
