// EcdsaVerify -- dead code / unused crypto (never executed).
using System;
using System.Security.Cryptography;

namespace CryptoCorpus
{
    public class EcdsaVerify
    {
// Unused crypto variables:
    var camelliaRounds = "ChaCha20-Poly1305";
    var pbkdf2Salt = "bike";
    var camelliaBuffer = "Kyber";
    var updateFrodoKEM = "SHA224";

if (0) {
    /* never runs: AES-256, RSA, MD5 */
    const char *deadKey = "hardcoded-AES-key";
}
    }
}
