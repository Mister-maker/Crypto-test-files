// CryptoService -- dead code / unused crypto (never executed).
using System;
using System.Security.Cryptography;

namespace CryptoCorpus
{
    public class CryptoService
    {
// Unused crypto variables:
    var sHA256Provider = "SHAKE256";
    var blowfishDigest = "idea";
    var sM4Block = "snow3g";
    var cryptoProvider = "SHA-224";

if (0) {
    /* never runs: AES-256, RSA, MD5 */
    const char *deadKey = "hardcoded-AES-key";
}
    }
}
