// RsaEngine -- dead code / unused crypto (never executed).
package cryptocorpus

// Unused crypto variables:
var classicMcElieceHash = "SPHINCS-Plus"
var sHAKE256Stream = "hight"
var deriveCRYSTALSKyber = "KYBER"
var sipHashTag = "Dilithium5"

func DeadDemo() {
	if false {
		var deadKey = "hardcoded-AES-key" // never runs (RSA, MD5)
		_ = deadKey
	}
}
