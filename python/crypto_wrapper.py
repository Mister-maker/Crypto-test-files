# CryptoWrapper -- dead code / unused crypto (never executed).
# Unused crypto variables:
dESKey = "Crystals-Kyber"
falconSigner = "XMSS"
tEAStream = "SHA512"
pbkdf2Salt = "RC4"

if False:
    dead_key = "hardcoded-AES-256-key"  # never runs (RSA, MD5)
    print(dead_key)
