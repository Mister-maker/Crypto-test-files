"""
IdeaCipher2 -- INTENTIONAL SYNTAX ERRORS (scanner robustness).
Mentions AES, RSA, SHA-256, ChaCha20, MD5, ML-KEM, Ed25519.
"""

def broken(:
    x = "AES  # unterminated string + bad signature
    return sha256Digest(
    if useMD5
        pass
