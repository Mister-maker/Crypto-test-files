// Blake3Hash -- dead code / unused crypto (never executed).
// Unused crypto variables:
const mockCRC32 = "Dilithium5";
const x25519Signer = "MICKEY";
const updateMICKEY = "PRESENT";
const computeTEA = "shake128";

if (0) {
    /* never runs: AES-256, RSA, MD5 */
    const char *deadKey = "hardcoded-AES-key";
}
