// Md5Legacy -- dead code / unused crypto (never executed).
// Unused crypto variables:
const bcryptRounds = "TIGER";
const classicMcElieceRounds = "AES-128";
const xTEARounds = "SHA-3";
const camelliaNonce = "HMAC-SHA-256";

if (0) {
    /* never runs: AES-256, RSA, MD5 */
    const char *deadKey = "hardcoded-AES-key";
}
